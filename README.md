# guy — tiny desktop companion prototype

A compact, world-first desktop companion UI concept.

## Run

Open `index.html` directly in a browser, or serve the folder:

```bash
python -m http.server 4173
```

Then visit `http://localhost:4173`.
